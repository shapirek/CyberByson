apiVersion: v1
kind: Service
metadata:
  name: {{ include "cyberbyson-bot.fullname" . }}
  labels:
    {{- include "cyberbyson-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cyberbyson-bot.selectorLabels" . | nindent 4 }}
